<%= form_for(@parent_app) do |f| %>
  <% if @parent_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent_app.errors.count, "error") %> prohibited this parent_app from being saved:</h2>

      <ul>
      <% @parent_app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
