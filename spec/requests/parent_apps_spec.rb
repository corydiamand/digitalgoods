require 'spec_helper'

describe "ParentApps" do
  describe "GET /parent_apps" do
    it "works! (now write some real specs)" do
      get parent_apps_path
      expect(response.status).to be(200)
    end
  end
end
